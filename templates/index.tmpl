<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title></title>
        <link href="css/style.css" rel="stylesheet">
    </head>
    <body>
        <h1>{{ .titulo }}</h1>
        <ul>
        {{range .canais}}
        <li> <a href="/home/canal/{{.Id}}/usuario/{{$.usuario.Username}}">{{.Nome}}</a></li>
        {{else}}
            <li>Nenhum item encontrado</li>
        {{end}}
        </ul>
    </body>
</html>
